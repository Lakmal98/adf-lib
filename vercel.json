{
  "builds": [
    {
      "src": "docs/mkdocs.yml",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "docs/site"
      }
    }
  ],
  "scripts": {
    "build": "python -m pip install -r docs/requirements.txt && cd docs && mkdocs build"
  }
}